{
  "_comment": [
    "What is expected of the next actor, one entry per factory:* state.",
    "",
    "This is FACTORY.md §3's 'Advanced by' column as data, so that the two",
    "engines say the same thing and say it every time. Both read this file",
    "out of the factory checkout they already make: the Actions pipeline via",
    "scripts/say_next_step.py, the orchestrator via",
    "factory_orchestrator.next_step. Change the wording here and both engines",
    "change together — never copy a line into an engine.",
    "",
    "Placeholders: {issue} the issue number, {approvers} the gate's approver",
    "list rendered as '@a, @b' (or 'any owner, member or collaborator' when",
    "the list is empty). 'who' is the actor; 'how' is the exact move, and it",
    "must name a real control — a comment to type, a PR to merge, a button,",
    "or a role to start. 'An agent will pick it up' is only true where the",
    "pipeline actually routes the state on its own.",
    "",
    "An optional 'expedited' variant per state carries the wording used when",
    "the issue's epic holds factory:expedite (FACTORY.md §4a) — there the",
    "factory advances the state itself and the reader is owed the opposite",
    "message: nothing to do. Renderers fall back to who/how when a state has",
    "no variant, which is right for every state expedite does not advance."
  ],
  "states": {
    "factory:backlog": {
      "who": "gate G0, on this issue's release tracker",
      "how": "This issue is parked until its milestone is approved. On the tracker issue for its milestone, a release_scope approver comments exactly `Approved`; every backlog issue in that milestone then moves into intake together."
    },
    "factory:release-planning": {
      "who": "any owner, member or collaborator",
      "how": "Comment exactly `Plan release` on this tracker. The Scrum Master reads the milestone, posts the release plan here and moves the tracker to `factory:release-ready`, which is gate G0."
    },
    "factory:release-ready": {
      "who": "gate G0 — {approvers}",
      "how": "Read the release plan above, then comment exactly `Approved` here. That releases every issue in the milestone into intake. There is no document PR at G0."
    },
    "factory:release-approved": {
      "who": "the factory",
      "how": "The milestone's issues are being moved into intake and their Intake Analysts started. Nothing to do."
    },
    "factory:intake": {
      "who": "the factory",
      "how": "The Intake Analyst is analysing the requirement and will open the spec PR. Nothing to do until it does."
    },
    "factory:spec-ready": {
      "who": "gate G1 — {approvers}",
      "how": "Review the spec PR this issue links, then either merge it, or comment exactly `Approved` here — the factory then merges it for you and starts the Planner. Strict match: \"Approved, but…\" is just a comment.",
      "expedited": {
        "who": "the factory — this epic is expedited",
        "how": "Gate G1 approves itself: the spec PR is merged and the Planner started, without waiting for you. The spec PR is still there to read, and removing `factory:expedite` puts the gates back from wherever the epic has got to."
      }
    },
    "factory:spec-approved": {
      "who": "the factory",
      "how": "The Planner is breaking the spec into task sub-issues, and chains the Architect in the same run. Nothing to do."
    },
    "factory:planned": {
      "who": "the factory",
      "how": "The Architect writes design.md and opens its PR — normally inside the Planner's own run. If this issue sits here with no run live, start it: the Factory Console's **Start the Architect** button on this issue, or Actions → \"Factory pipeline\" → Run workflow (role: `architect`, issue `{issue}`)."
    },
    "factory:design-ready": {
      "who": "gate G2 — {approvers}",
      "how": "Review the design PR(s) this issue links, then either merge them, or comment exactly `Approved` here — the factory then merges and releases the unblocked tasks for implementation.",
      "expedited": {
        "who": "the factory — this epic is expedited",
        "how": "Gate G2 approves itself: the design PR(s) are merged, the Dispatcher runs, and every unblocked task starts its Implementer without waiting for you. Remove `factory:expedite` to put the gates back."
      }
    },
    "factory:design-approved": {
      "who": "the factory",
      "how": "The Dispatcher is marking every unblocked task `factory:ready`. Nothing to do until a task asks for its start approval."
    },
    "factory:ready": {
      "who": "the implementation approvers — {approvers}",
      "how": "Comment exactly `Approved` on this task to start its Implementer, or press **Approve** on it in the Factory Console. Only one start per task: while a run is live the approval is declined, because a second one would put a second Implementer on the same branch.",
      "expedited": {
        "who": "the factory — this task's epic is expedited",
        "how": "Its Implementer starts on its own; no `Approved` is needed. If the Reviewer sends this task back here, the next Implementer starts automatically too — until the two-round rework cap, which stops at `factory:blocked` and asks for you."
      }
    },
    "factory:in-review": {
      "who": "a human, then the Reviewer",
      "how": "`factory:in-review` starts nothing on its own. Start the review — the Factory Console's **Start the Reviewer** button on this task, or Actions → \"Factory pipeline\" → Run workflow (role: `reviewer`, issue `{issue}`). The Reviewer reads the draft PR this task links and moves it to `factory:in-test`, or back to `factory:ready` with changes requested. Reviewed it yourself instead? Comment exactly `Review Done` here to skip the Reviewer and move straight to `factory:in-test` — there is no changes-requested equivalent for that path; if you found problems, review the draft PR directly on GitHub. Do not merge the draft PR yourself either way: review and QA are what make it mergeable, and the Release Manager merges it.",
      "expedited": {
        "who": "the factory — this task's epic is expedited",
        "how": "The Reviewer starts on its own; nothing here needs you. Its verdict moves this task to `factory:in-test`, or back to `factory:ready` for another round. The draft PR is there to read if you want to review it yourself — `Review Done` still works and still skips the Reviewer."
      }
    },
    "factory:in-test": {
      "who": "a human, then QA",
      "how": "`factory:in-test` starts nothing on its own. Start QA — the Factory Console's **Start QA** button on this task, or Actions → \"Factory pipeline\" → Run workflow (role: `qa`, issue `{issue}`). QA verifies the WHEN/THEN scenarios, posts the test report here and moves the task to `factory:ready-to-ship`.",
      "expedited": {
        "who": "the factory — this task's epic is expedited",
        "how": "QA starts on its own and posts its test report here. Nothing needs you unless it stops at `factory:blocked`."
      }
    },
    "factory:ready-to-ship": {
      "who": "a human, then the Release Manager",
      "how": "`factory:ready-to-ship` starts nothing on its own. Start the Release Manager — the Factory Console's **Start the Release Manager** button on this task, or Actions → \"Factory pipeline\" → Run workflow (role: `release`, issue `{issue}`). It merges this task onto its epic branch and verifies it there. Where the epic has no epic branch there is nothing to merge onto yet: the task waits here, because its only next branch is the integration branch and that merge is the staging deploy, which happens behind gate GS once the whole epic is ready.",
      "expedited": {
        "who": "the factory, or you — it depends on this repo's branch policy",
        "how": "With epic branches on (`\"epics\": true`), the Release Manager starts on its own and merges this task onto the epic branch — nothing needs you. With epic branches off, the expedite chain deliberately stops here: the next merge would land on the integration branch and deploy staging, which is never automatic. Start the Release Manager yourself then — the Console's **Start the Release Manager** button, or Run workflow (role: `release`, issue `{issue}`)."
      }
    },
    "factory:on-epic": {
      "who": "the rest of the epic",
      "how": "This task is merged onto its epic branch and green there, and it is done: nothing further happens to it on its own. It waits for its siblings — when the last one lands, the **epic** goes to `factory:epic-ready` and asks for gate GS there, not here."
    },
    "factory:epic-ready": {
      "who": "gate GS — {approvers}",
      "how": "Every task in this epic is implemented, reviewed, tested and assembled on the epic branch, and none of it has touched staging yet. Comment exactly `Approved` here (or press **Approve** in the Factory Console) and the Release Manager carries the whole epic to the integration branch and verifies it there. That is a staging release, not a production one — production is still gate G3, a human merging the promotion PR."
    },
    "factory:in-staging": {
      "who": "gate G3 — {approvers}",
      "how": "The Release Manager's integration report above lists the promotion PR(s) and the order to merge them. Merge them through the GitHub UI, in that order. G3 is deliberately not comment-approvable — `Approved` does nothing here."
    },
    "factory:deployed": {
      "who": "Ops",
      "how": "In production and soaking. Ops verifies health, archives the change folder and closes this issue — the Factory Console's **Start Ops** button, or Actions → \"Factory pipeline\" → Run workflow (role: `ops`, issue `{issue}`). File `factory:incident` instead if the deploy regressed."
    },
    "factory:fast-track": {
      "who": "any owner, member or collaborator",
      "how": "The fast lane has no gate and no reviewer: read the PR this issue links and merge it, or say what needs changing here. Nothing else runs on this issue."
    },
    "factory:blocked": {
      "who": "any owner, member or collaborator",
      "how": "An agent stopped and asked a question — it is the newest agent comment on this thread. Reply here with the answer: the factory clears `factory:blocked` and re-runs the stage that halted, re-reading the whole thread."
    },
    "factory:incident": {
      "who": "a human, with the Release Manager",
      "how": "A post-deploy regression is open. Decide rollback or fix-forward and say which here; the Release Manager carries it out."
    },
    "factory:profile": {
      "who": "the Profiler",
      "how": "The Profiler is drafting `.factory/profile.json` and removes this label when it finishes. Re-apply the label to ask for a redraft."
    }
  },
  "none": {
    "who": "nobody — this issue is off the rail",
    "how": "It carries no `factory:*` state label, so nothing routes it. Apply the state it belongs in (`factory:intake` to start it from the top), or leave it: an issue without a state is not the factory's."
  }
}
