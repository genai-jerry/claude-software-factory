{
  "$comment": "Copy to .github/factory-orchestrator.json to choose which engine drives this repo's factory pipeline. Absent file (or engine: github-actions) keeps the Actions pipeline. Naming an external engine (langgraph) makes the Actions route job stand down; the external engine refuses repos whose config does not name it, so exactly one engine acts per event. endpoint and runners are read by the external engine only.",
  "engine": "github-actions",
  "endpoint": "",
  "runners": {
    "max_parallel": 4
  }
}
